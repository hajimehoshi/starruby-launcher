name = star-ruby-launcher

[main.d]
target = star-ruby-launcher
buildflags = -gui -I$PREFIX/import -w
version (release) {
  prebuild = echo Building Release Version
  buildflags += -O -release -inline
} else {
  prebuild = echo Building Debug Version
  buildflags += -g -debug -unittest
}
